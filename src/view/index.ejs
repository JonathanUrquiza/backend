<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
    <meta name="description" content="<%= view.description %>">
    <meta name="keywords" content="<%= view.keywords %>">
    <meta name="author" content="<%= view.author %>">
    <meta name="year" content="<%= view.year %>">
    <title><%= view.title %></title>
</head>
<body>
    <% if (view.header) { %>
        <nav class="navbar navbar-expand-lg navbar-light bg-light">
            <div class="container-fluid">
                <a class="navbar-brand" href="#">Monolito</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
            </div>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link" href="/">Inicio</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/upload">Cargar Imágenes</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/contacto">Contacto</a>
                    </li>
                </ul>
            </div>
        </nav>
    <% } %>

    <main class="container">
        <% if (view.pageType === 'welcome' || !view.pageType) { %>
            <!-- Página de Bienvenida -->
            <section class="row">
                <div class="col-12">
                    <div class="jumbotron bg-primary text-white text-center p-5 rounded mb-4">
                        <h1 class="display-4">¡Bienvenido!</h1>
                        <p class="lead">Bienvenido al Monolito de Fiscalización</p>
                        <hr class="my-4 bg-white">
                        <p>Selecciona una opción del menú para comenzar</p>
                        <div class="mt-3">
                            <a class="btn btn-light btn-lg me-2" href="/upload" role="button">Cargar Imágenes</a>
                            <a class="btn btn-outline-light btn-lg" href="/contacto" role="button">Contacto</a>
                        </div>
                    </div>
                </div>
            </section>
        <% } else if (view.pageType === 'upload' || view.pageType === 'images') { %>
            <!-- Página de Carga de Imágenes -->
            <%- include('imageUploadForm') %>
        <% } else { %>
            <!-- Página por defecto o error -->
            <section class="row">
                <div class="col-12">
                    <div class="alert alert-warning text-center" role="alert">
                        <h4 class="alert-heading">Página no encontrada</h4>
                        <p>La página solicitada no existe o no está disponible.</p>
                        <hr>
                        <div class="mt-3">
                            <a href="/" class="btn btn-primary">Ir al Inicio</a>
                        </div>
                    </div>
                </div>
            </section>
        <% } %>
    </main>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>